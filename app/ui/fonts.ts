import { Work_Sans, Lusitana } from 'next/font/google';
 
export const WorkSans = Work_Sans({ subsets: ['latin'], display: 'swap' });

export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
  });